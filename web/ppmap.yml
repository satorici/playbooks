settings:
  name: "Ppmap - scanner/exploitation tool that exploits known and existing gadgets (checks for specific variables in the global context) to perform XSS via Prototype Pollution"
  image: ubuntu:jammy
  author: 
  - https://github.com/kleiton0x00
  gallery:
  - https://files.catbox.moe/deptae.png
  example: satori run satori://web/ppmap.yml -d URL="https://satori.ci" --output
  
install:
  - apt update
  - apt install -qy git golang sudo
  - git clone https://github.com/kleiton0x00/ppmap.git
  - cd ppmap/ && bash setup.sh

ppmap:
  assertStdoutNotContains: 
  - "VULN"
  - "EXPL"
  run:
  - echo '${{URL}}' | ppmap
