#V2
settings:
  name: Test shards
  files: true

setShell: true

echo:
  - echo "id:$SATORI_COUNT_INDEX" >> zgrab2.json
  - echo "shards:$SATORI_COUNT,port:${{PORT}},range=${{RANGE}}" >> all-found.txt
