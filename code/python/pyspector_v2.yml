settings:
  name: Pyspector v2

install:
  - apt-get update && apt-get install -y python3-pip
  - pip3 install pyspector

test:
  assertStdoutContains: '"issue_count": 0'
  run:
    - pyspector scan -f json ./
