settings:
  name: "OpenAI gpt-oss:20b"
  description: "Runs the OpenAI gpt-oss:20b model locally using Ollama to query with custom input."
  image: ollama/ollama
  cpu: 16384
  memory: 122880
  timeout: 400
  author:
    - https://github.com/ollama
  example: satori run satori://llm/openai.yml -d INPUT="Hello World" --report --output

MODEL:
  - - "gpt-oss:20b"

openai:
  install:
    - apt-get update >> /dev/null; apt-get install -qy screen >> /dev/null

  test:
    assertReturnCode: 0
    serve:
      - screen -dm ollama serve
      - sleep 1

    pull:
      - ollama pull ${{MODEL}}

    query:
      - ollama run ${{MODEL}} "${{INPUT}}"
