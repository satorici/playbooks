settings:
  name: "docker_password: Find Docker login passwords"
  image: debian
  author:
    - https://github.com/satorici
  example: satori run ./ --playbook satori://secrets/docker_password.yml --report --output

docker_password:
  assertStdout: False
  run:
    - grep "docker login.* -p.*" * -r
